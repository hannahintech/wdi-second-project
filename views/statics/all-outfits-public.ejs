<div class="container test">
  <h1>items display</h1>
  <ul class="container">
    <div class="row">
    <% items.forEach((item) => { %>
      <% console.log(item) %>
      <% console.log(items) %>
      <li class"col-3">
      <img src="<%= item.category[0] %>">
        <h3>Generated by: to come</h3>//(id of user who generated this => attained from "generate outfit button" view G, && saved "button clicked" view H)
      </li>
    <% }) %>
    </div>
  </ul>
</div>
